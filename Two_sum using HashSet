class Solution {
    boolean twoSum(int arr[], int target) {
        HashSet<Integer> s = new HashSet<>();
        for (int num : arr) {
            if (s.contains(target - num)) {
                return true;
            }
            s.add(num);
        }
        return false;
    }
}

        
